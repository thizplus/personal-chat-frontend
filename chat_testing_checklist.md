# 🧪 Chat System Testing Checklist

> **วัตถุประสงค์:** ทดสอบว่าระบบ Chat ทำงานตรงตาม `chat_system_flow_analysis.md` หรือไม่
>
> **URL ทดสอบ:** http://localhost:5173/chat/69cd966b-c0f4-44bf-ae6f-f08eaf501e20

---

## 📝 วิธีการใช้งาน Checklist

1. เปิด Chrome DevTools (F12)
2. ไปที่แท็บ **Console** และ **Network**
3. ทดสอบทีละข้อตามลำดับด้านล่าง
4. ✅ = ผ่าน, ❌ = ไม่ผ่าน, ⚠️ = มีปัญหาบางส่วน
5. เมื่อเจอข้อที่ไม่ผ่าน → บันทึก screenshot / console log / network request

---

## 🎯 Test Section 1: Initial Chat Load

### Test 1.1: API Request ถูกต้อง
**ขั้นตอน:**
1. Clear console และ network tab
2. Refresh หน้า (F5) หรือคลิกเข้า conversation ใหม่
3. ดูที่ Network tab

**ตรวจสอบ:**
- [x] มี Request: `GET /api/v1/conversations/{conversationId}/messages`
- [x] Query parameters: `?limit=20` (หรือมี limit parameter)
- [x] Response status: `200 OK`
- [x] Response มี structure: `{ success: true, data: { messages: [], has_more: boolean } }`

**หากไม่ผ่าน → บันทึก:**
- Request URL ที่เห็นจริง: ______________________
- Query params ที่เห็นจริง: ______________________
- Response structure: ______________________

---

### Test 1.2: Messages แสดงผลถูกต้อง
**ขั้นตอน:**
1. หลังจากโหลดเสร็จ ดูข้อความในหน้าจอ

**ตรวจสอบ:**
- [x] ข้อความแสดงผลเรียงจาก **เก่าสุด → ใหม่สุด** (ASC)
- [x] Scroll อยู่ที่ **ล่างสุด** (ข้อความล่าสุด)
- [ ] จำนวนข้อความที่แสดง = จำนวนที่ได้จาก API response
- [x] ไม่มีข้อความซ้ำ

**หากไม่ผ่าน → บันทึก:**
- ข้อความเรียงอย่างไร: ______________________
- Scroll อยู่ตรงไหน: ______________________
- มีข้อความซ้ำไหม: ______________________
- จำนวนข้อความที่แสดง : ใน DOM มี 16 อันครับเรียงจากบน-ลงล่าง data-index 24-39  DOM ล่างสุดคือ data-index 39 คือข้อความใหม่ล่าสุด
---

### Test 1.3: Console Logs (Debug)
**ขั้นตอน:**
1. ดูที่ Console

**ตรวจสอบ:**
- [x] เห็น log: `[debug_scroll] 🔄 Conversation changed, reinitializing...`
- [x] เห็น log: `[MessageItem] Rendering: ...` (หลายบรรทัด)
- [x] ไม่มี error สีแดง

**หากไม่ผ่าน → บันทึก:**
- Error ที่เห็น: ______________________

---

## 🔼 Test Section 2: Load More (Scroll Up)

### Test 2.1: Trigger Load More
**ขั้นตอน:**
1. Clear console
2. Scroll ขึ้นไปด้านบน **ช้าๆ** จนถึงขอบบนสุด
3. รอ 1-2 วินาที

**ตรวจสอบ:**
- [x] เห็น log: `[debug_scroll] ⬆️ Load more triggered`
- [x] เห็น log: `[debug_scroll] 🔝 atTopStateChange: true`
- [x] มี spinner หรือ loading indicator

**หากไม่ผ่าน → บันทึก:**
- Scroll ถึงขอบบนแล้วแต่ไม่เห็น log: ______________________
- ต้อง scroll ซ้ำหลายครั้งไหม: ______________________

---

### Test 2.2: API Request Load More
**ขั้นตอน:**
1. ดูที่ Network tab หลังจาก trigger load more

**ตรวจสอบ:**
- [x] มี Request: `GET /api/v1/conversations/{conversationId}/messages`
- [x] มี Query param: `?before={firstMessageId}&limit=20`
- [x] `before` คือ ID ของข้อความแรกสุดที่มีอยู่ (มันคือข้อความอันดับแรกสุดของ request ก่อนหน้าครับ)
- [x] Response status: `200 OK`
- [x] Response มี `messages[]` array

**หากไม่ผ่าน → บันทึก:**
- Query params ที่เห็นจริง: ______________________
- มี `before` parameter หรือไม่: 
---

### Test 2.3: Messages Prepend ถูกต้อง
**ขั้นตอน:**
1. หลังจาก API response กลับมา
2. ดูข้อความในหน้าจอ

**ตรวจสอบ:**
- [x] มีข้อความใหม่เพิ่มขึ้น **ด้านบน**
- [x] ข้อความเก่าที่มีอยู่ยังคงอยู่ **ด้านล่าง**
- [ ] ไม่มีการ scroll กระโดด (scroll position คงที่)
- [x] เห็น log: `[debug_scroll] 📥 Prepending X messages at top`
- [x] เห็น log: `[debug_scroll] ✅ firstItemIndex: 100000 -> 9998X` (ลดลง)

**หากไม่ผ่าน → บันทึก:**
- Scroll กระโดดไหม: มันไม่เชิงกระโดยครับ เหมือนตตอนโหลดเนื้อหามันหายไปนิดนึงแล้วก็กลับมา แต่ก็อยู่ไกล้เคียงตำแหน่งเดิมครับ ถ้ามองตัวตาเปล่ามันก็รู้สึกว่ากระพริบนิดๆ
- ข้อความเพิ่มที่ไหน: ______________________
- firstItemIndex เปลี่ยนแปลงไหม: ______________________

---

### Test 2.4: Images/Stickers ไม่ Skeleton Flash
**ขั้นตอน:**
1. Load more จนเห็นข้อความที่มีรูปภาพหรือ sticker
2. สังเกตว่ารูปแสดงผลอย่างไร

**ตรวจสอบ:**
- [x] รูปภาพ/sticker ที่เคยโหลดแล้ว **ไม่แสดง skeleton** อีก
- [x] รูปภาพใหม่แสดง skeleton ชั่วคราว แล้วค่อยแสดงรูป
- [ ] ไม่มีการกระพริบ

**หากไม่ผ่าน → บันทึก:**
- รูปเก่ากระพริบไหม: รูปไม่กระพริบครับ แต่มันเหมือนกระพริบทั้งหมดในหน้าจอ ไม่ใช่แค่ภาพครับ

---

### Test 2.5: Load More หลายครั้ง
**ขั้นตอน:**
1. Scroll ขึ้นไปเรื่อยๆ
2. ทดสอบ load more 3-4 รอบ

**ตรวจสอบ:**
- [ ] แต่ละรอบโหลดได้ปกติ ไม่ต้อง scroll ซ้ำ
- [x] ไม่มี duplicate API calls (ไม่เรียก API ซ้ำก่อนรอบแรกเสร็จ)
- [x] เมื่อ `has_more: false` → หยุดโหลด และไม่เรียก API อีก

**หากไม่ผ่าน → บันทึก:**
- ต้อง scroll ซ้ำไหม: ต้อง scroll ซ้ำครับ  บางที scroll เร็วๆมันค้าง ต้อง scroll ลงมาข้างล่างแล้ว scroll ขึ้นบนใหม่อีกครั้ง
- API ถูกเรียกซ้ำไหม: ______________________

---

## 🎯 Test Section 3: Jump to Message

> **Note:** ต้องมีฟีเจอร์ Jump to Message ใน UI ก่อน (เช่น Reply message → คลิกเพื่อ jump)

### Test 3.1: Jump to Message ที่มีอยู่แล้ว
**ขั้นตอน:**
1. โหลดแชท (มีข้อความ #350-#370 อยู่ในหน้าจอ)
2. Jump ไปข้อความ #360 (ที่มีอยู่แล้ว)

(ผมทดสอบด้วยการเลื่อน scroll ให้โหลด message ทั้งหมดก่อนนะครับ ตำแหน่งปัจจุบันของผมคือข้อความเก่าสุด บนสุด)

**ตรวจสอบ:**
- [x] Scroll ไปที่ข้อความ #360 ทันที (smooth scroll)
- [x] ข้อความ #360 อยู่ตรงกลางหน้าจอ
- [x] มี highlight animation (ring-yellow-400) 1-2 วินาที
- [ ] **ไม่มี** API call ใหม่

**หากไม่ผ่าน → บันทึก:**
- มี API call ไหม: มี api call ครับ http://localhost:8080/api/v1/conversations/69cd966b-c0f4-44bf-ae6f-f08eaf501e20/messages/context?targetId=3e85d04f-c4ef-4c50-9f2d-32de1da51b38&before=20&after=20

- Scroll ไปถูกข้อความไหม: ______________________

---

### Test 3.2: Jump to Message ที่ยังไม่มี (ไกลมาก)
**ขั้นตอน:**
1. โหลดแชท (มีข้อความ #350-#370)
2. Jump ไปข้อความ #50 (ที่ยังไม่มีใน memory)

(ผมทดสอบด้วยการ กด f5 เพื่อ refresh หน้าจอใหม่ แล้วผมจะ jump ไปที่ข้อความกลางจอครับ)
http://localhost:8080/api/v1/conversations/69cd966b-c0f4-44bf-ae6f-f08eaf501e20/messages/context?targetId=727a394e-4b5c-4748-a1c2-f43ebf7fb61a&before=20&after=20
**ตรวจสอบ:**
- [x] มี API call: `GET /api/v1/conversations/{conversationId}/messages/context`
- [x] Query params: `?targetId={messageId}&before=20&after=20`
- [ ] Response มี structure: `{ success: true, data: { data: [], has_before: boolean, has_after: boolean } }`
- [x] หลัง API กลับมา → ข้อความทั้งหมด **replace** เป็น context รอบๆ #50
- [ ] Scroll ไปที่ข้อความ #50 (กลางหน้าจอ)
- [x] มี highlight animation

**หากไม่ผ่าน → บันทึก:**
- API endpoint ที่เรียกจริง: ______________________
- Response structure: {"data": [],"has_after": true,"has_before": true,"success": true} ไม่เหมือนตามโครงสร้างที่คุณแจ้งครับ
- ข้อความ replace หรือ append: ข้อความจะถูก replace ใน DOM ครับ
- ทดสอบสอบด้วยการกด f5 แล้วลอง jump ครั้งแรก jump แล้วไปไม่ตรงตามตำแหน่ง
ผมลองทดสอบด้วยการ f5 แล้วเลื่อน scroll เพียงนิดเดียว (ไม่ได้ไปถึง loadmore) แบบนี้มันไปถูกตำแหน่งครับ แต่มีบางทีเหมือนเลยตำแหน่ง แต่ก็ smooth scroll กลับมาได้ตรง

---

### Test 3.3: State Flags หลัง Jump
**ขั้นตอน:**
1. หลังจาก jump ไป #50 เสร็จ
2. ดูที่ Console logs

**ตรวจสอบ:**
- [ ] `hasMoreMessages` ควรเป็น `true` (เพราะมีข้อความก่อน #50)
- [ ] `hasAfterMessages` ควรเป็น `true` (เพราะมีข้อความหลัง #50)

**หากไม่ผ่าน:**
- ไม่มี flag นี้ในระบบ หรือค่าไม่ถูกต้อง
ใน console log ไม่เจอ ทั้ง hasMoreMessages และ hasAfterMessages ครับ แต่มันมีการเรียก api จริง
http://localhost:8080/api/v1/conversations/69cd966b-c0f4-44bf-ae6f-f08eaf501e20/messages/context?targetId=727a394e-4b5c-4748-a1c2-f43ebf7fb61a&before=20&after=20 ที่มี 
has_after: true
has_before: true
---

## 🔽 Test Section 4: After Jump - Scroll Up

### Test 4.1: Scroll Up หลัง Jump
**ขั้นตอน:**
1. Jump ไปข้อความ #50 แล้ว
2. Scroll **ขึ้น** ไปด้านบน

**ตรวจสอบ:**
- [x] Trigger load more ปกติ (atTopStateChange)
- [x] API call: `GET /messages?before={firstMessageId}&limit=20`
- [x] Prepend messages ด้านบน

**หากไม่ผ่าน → บันทึก:**
- Load more ทำงานไหม: ______________________
- แต่รู้สึกว่ามันกระตุกๆ กระพริบๆครับ ไม่ค่อย smooth
---

## 🔽 Test Section 5: After Jump - Scroll Down

### Test 5.1: Scroll Down หลัง Jump
**ขั้นตอน:**
1. Jump ไปข้อความ #50 แล้ว
2. Scroll **ลง** ไปด้านล่าง

**ตรวจสอบ:**
- [ ] มี callback `onLoadMoreAtBottom` trigger
- [ ] เห็น log หรือ API call: `GET /messages?after={lastMessageId}&limit=20`
- [ ] Append messages ด้านล่าง

**หากไม่ผ่าน → บันทึก:**
- ไม่มีฟีเจอร์นี้ หรือไม่ทำงาน: ยังไม่มีฟีเจอร์นี้
เลื่อนลงมาไม่เกิดอะไรขึ้นเลยครับ ไม่มี console log onLoadMoreAtBottom และไม่มีการเรียก api

---

## 📱 Test Section 6: Mobile Compatibility

### Test 6.1: Mobile Access
**ขั้นตอน:**
1. เปิด Chrome DevTools → Toggle device toolbar (Ctrl+Shift+M)
2. เลือก iPhone หรือ Android
3. เข้า URL: http://localhost:5173/chat/69cd966b-c0f4-44bf-ae6f-f08eaf501e20

**ตรวจสอบ:**
- [x] เข้าหน้าได้ ไม่ redirect loop
- [x] แสดงข้อความได้ปกติ
- [x] Scroll ขึ้น-ลงได้
- [x] Load more ทำงาน

**หากไม่ผ่าน → บันทึก:**
- Redirect ไปไหน: ______________________
- Console error: ______________________

---

## 🎨 Test Section 7: Performance & UX

### Test 7.1: Smooth Scrolling
**ขั้นตอน:**
1. Scroll ขึ้น-ลงแบบต่อเนื่อง
2. สังเกตความลื่นไหล

**ตรวจสอบ:**
- [ ] ไม่กระตุก (jank)
- [ ] ไม่ค้าง
- [ ] Frame rate สม่ำเสมอ (ดูจาก DevTools Performance tab ถ้าต้องการ)

**หากไม่ผ่าน → บันทึก:**
- อาการที่เห็น: ถ้าเลื่อน scroll เร็วๆ ก้มีค้าง ต้อง scroll ลงมา แล้ว scroll ขึ้นไปใหม่
- กระตุกกระพริบเฉพาะตอน loadmore ครับ
- อาการที่มันกระตุกนะครับ คือจังหวะที่ loadmore ครับ  ผมลองทดสอบจาก internet ที่ช้า เลยเห็นความกระตุก กระพริบนั้นครับ  อาการคือ dom ปัจจุบันเหมือนหายไปครับ dom ล่างสุดมันเลยขึ้นมาแทนแปปนึง จากนั้น dom ใหม่เข้ามาแทนพร้อมกับ message ใหม่ที่เลื่อนขึ้นไปต่อได้ครับ

มันเป็นอาการปกติของ virtualized list เวลา prepend ข้อมูลใหม่ โดยเฉพาะ network ช้า + item height ต่างกัน แต่สามารถลด/แก้ได้ด้วยการรักษา scroll offset และกำหนด height ที่แน่นอน ผมเข้าใจนะครับ ว่าเราอาจจะแก้ไขอะไรได้ไม่มากนัก

---

### Test 7.2: GIF Sticker Performance
**ขั้นตอน:**
1. Scroll ผ่านข้อความที่มี GIF sticker หลายๆ อัน

**ตรวจสอบ:**
- [x] GIF เล่นได้ปกติ
- [ ] เมื่อ scroll ออกจากหน้าจอ → GIF หยุด (visibility: hidden)
- [x] เมื่อ scroll กลับมา → GIF เล่นต่อ

**หากไม่ผ่าน → บันทึก:**
- GIF ทำงานอย่างไร: เมื่อ scroll ออกจากหน้าจอ ผมไม่แน่ใจว่ามมัน hidden ไหม แต่ dom มันหายไป แต่ใน tab network มี request หลายอันมา gif เหมือนเรียกซ้ำๆด้วย 2-3 อันในภาพเดียว ไม่แน่ใจเพราะอะไร

---

### Test 7.3: Image Loading
**ขั้นตอน:**
1. Scroll ไปที่ข้อความที่มีรูปภาพ

**ตรวจสอบ:**
- [x] แสดง skeleton loader ก่อน
- [ ] โหลดเสร็จแล้วแสดงรูป (smooth fade-in)
- [ ] มี aspect ratio reserved (ไม่ layout shift)

**หากไม่ผ่าน → บันทึก:**
- Layout shift เกิดขึ้นไหม: (CLS) 6.87

---

## 🐛 Test Section 8: Edge Cases

### Test 8.1: Empty Conversation
**ขั้นตอน:**
1. เข้า conversation ที่ไม่มีข้อความเลย

**ตรวจสอบ:**
- [x] แสดงข้อความ "ยังไม่มีข้อความ"
- [x] ไม่มี error
- [x] ไม่เรียก API load more

**หากไม่ผ่าน → บันทึก:**
- แสดงอะไร: ______________________

---

### Test 8.2: No More Messages
**ขั้นตอน:**
1. Scroll ขึ้นจนโหลดหมด (has_more: false)
2. Scroll ขึ้นต่อ

**ตรวจสอบ:**
- [x] ไม่เรียก API อีก
- [x] ไม่มี infinite loop

**หากไม่ผ่าน → บันทึก:**
- เรียก API ต่อไหม: ______________________

---

### Test 8.3: Duplicate Messages
**ขั้นตอน:**
1. Scroll ขึ้น-ลงหลายครั้ง
2. สังเกตข้อความ

**ตรวจสอบ:**
- [x] ไม่มีข้อความซ้ำแม้โหลดหลายรอบ
- [x] Deduplication ทำงาน (localKey pattern)

**หากไม่ผ่าน → บันทึก:**
- เห็นข้อความซ้ำไหม: ไม่มีข้อความซ้ำครับ

---

## ✅ Summary

### จำนวนข้อที่ผ่าน:
- Section 1 (Initial Load): ___/12
- Section 2 (Load More Up): ___/15
- Section 3 (Jump): ___/9
- Section 4 (After Jump Up): ___/3
- Section 5 (After Jump Down): ___/3
- Section 6 (Mobile): ___/4
- Section 7 (Performance): ___/9
- Section 8 (Edge Cases): ___/9

**Total: ___/64**

---

## 🔍 ปัญหาที่พบ (Summary)

กรอกปัญหาที่พบจาก checklist ข้างบน:

### ❌ Critical Issues (ระบบใช้งานไม่ได้)
1. scroll down หลัง jump to message ครับ
2. ______________________

### ⚠️ Major Issues (ใช้งานได้แต่มีปัญหา)
1. กระพริบ load more จาก dom
2. jump ไม่ smooth เท่าไหร่
3. หลัง jump แล้ว scroll ไม่ smooth เท่าไหร่ครับ

### 🐞 Minor Issues (ปัญหาเล็กน้อย)
1. ______________________
2. ______________________

---

## 📝 Next Steps

1. รวบรวมปัญหาทั้งหมดจาก checklist
2. ส่งข้อมูลต่อไปนี้:
   - ข้อที่ไม่ผ่าน
   - Screenshot / Console logs
   - Network requests (Copy as cURL)
3. วิเคราะห์และแก้ไขตามลำดับความสำคัญ

---

**หมายเหตุ:** ถ้าฟีเจอร์ไหนยังไม่ได้ implement (เช่น Jump to Message) ให้ระบุว่า "ยังไม่มีฟีเจอร์นี้" แทนที่จะเป็น "ไม่ผ่าน"
