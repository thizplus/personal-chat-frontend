// src/constants/api/index.ts
import standardApi from './standardApiConstants';

/**
 * API constants รวมทั้งหมดสำหรับใช้ในแอปพลิเคชัน
 */
export const API = {
  // บัญชีผู้ใช้ทั่วไป
  ...standardApi,
};

export default API;