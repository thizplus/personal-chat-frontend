# คู่มือการทดสอบ Virtua POC - Testing Guide

## 🎯 วัตถุประสงค์ (Purpose)

ทดสอบ Virtua library กับ message หลายประเภท (text, image, file, sticker) เพื่อตรวจสอบ:
- **DOM Overlapping** - DOM ทับกันหรือไม่
- **Dynamic Height Handling** - จัดการความสูงแบบ dynamic ได้ดีไหม
- **Scroll Performance** - scroll smooth ไหม เมื่อมี message เยอะๆ
- **Memory Usage** - ใช้ memory เท่าไหร่เมื่อมี message หลายพันข้อความ

---

## 🚀 วิธีเข้าใช้งาน (How to Access)

### หน้าทดสอบใหม่ (Enhanced Testing POC)

```
http://localhost:5173/poc/chat-virtua-enhanced/69cd966b-c0f4-44bf-ae6f-f08eaf501e20
```

หรือใช้ conversationId ใดก็ได้ที่คุณมีอยู่ในระบบ

### หน้าทดสอบเดิม (Original POC)

```
http://localhost:5173/poc/chat-virtua/69cd966b-c0f4-44bf-ae6f-f08eaf501e20
```

---

## 🛠️ ฟีเจอร์ของหน้าทดสอบ (Testing Features)

### 1. โหมดการทำงาน 2 แบบ (Two Modes)

#### **Backend Mode** (ใช้ข้อมูลจริงจาก API)
- แสดง message จริงจากฐานข้อมูล
- ทดสอบกับข้อมูลจริงที่มี conversation
- รองรับ load more messages

#### **Local Test Mode** (ใช้ mock data สำหรับทดสอบ)
- สร้าง message ทดสอบได้ไม่จำกัด
- ไม่ต้องพึ่ง backend
- เหมาะสำหรับทดสอบ performance และ DOM overlapping

**วิธีสลับโหมด:**
- คลิกปุ่ม "Use Local Test" / "Use Backend" ที่มุมขวาบน

---

## 📝 วิธีทดสอบแบบละเอียด (Detailed Testing Steps)

### Test Case 1: ทดสอบ Message แต่ละประเภท

**วัตถุประสงค์:** ตรวจสอบว่าแต่ละประเภท message render ถูกต้องและไม่มี DOM ทับกัน

**ขั้นตอน:**

1. **เปิดหน้าทดสอบ:**
   ```
   http://localhost:5173/poc/chat-virtua-enhanced/test-conv-001
   ```

2. **สลับเป็น Local Test Mode:**
   - คลิก "Use Local Test" ที่มุมขวาบน

3. **เพิ่ม Text Message:**
   - คลิกปุ่ม "Add Text"
   - สังเกต: ข้อความแสดงถูกต้อง, ความสูง dynamic ตามเนื้อหา
   - ✅ **Expected:** Message แสดงครบ, ไม่มี overlap

4. **เพิ่ม Image Message:**
   - คลิกปุ่ม "Add Image"
   - สังเกต: รูปภาพโหลดและแสดงผล (240px width, aspect 4/3)
   - คลิกที่รูปเพื่อเปิด lightbox
   - ✅ **Expected:** รูปโหลดเสร็จ, ไม่ทับ message อื่น

5. **เพิ่ม File Message:**
   - คลิกปุ่ม "Add File"
   - สังเกต: แสดง file icon, ชื่อไฟล์, ขนาดไฟล์
   - คลิกที่ file message เพื่อ download (จะเปิด tab ใหม่)
   - ✅ **Expected:** File info แสดงครบ, fixed height ~80px

6. **เพิ่ม Sticker Message:**
   - คลิกปุ่ม "Add Sticker"
   - สังเกต: สติกเกอร์แสดง (120x120px), อาจเป็น GIF เคลื่อนไหว
   - ✅ **Expected:** Sticker แสดงถูกขนาด, ไม่ยืด/หด

**ตรวจสอบ:**
- [ ] Text messages ไม่ทับกัน
- [ ] Image messages โหลดครบ และไม่ push content ลง
- [ ] File messages แสดง metadata ครบ
- [ ] Sticker messages แสดงถูกขนาด
- [ ] Spacing ระหว่าง messages สม่ำเสมอ

---

### Test Case 2: ทดสอบ Mixed Messages (10-50 ข้อความ)

**วัตถุประสงค์:** ตรวจสอบว่า Virtua จัดการ mixed content types ได้ดีไหม

**ขั้นตอน:**

1. **เริ่มต้นใหม่:**
   - คลิก "Clear All" (ถ้ามี message เก่า)

2. **เพิ่ม 10 Mixed Messages:**
   - คลิกปุ่ม "Add 10 Mixed"
   - สังเกต: messages ปรากฏทีละข้อความอย่างรวดเร็ว
   - Scroll ดูทุก message

3. **ตรวจสอบ DOM:**
   - เปิด Chrome DevTools (F12)
   - ไปที่ tab "Elements"
   - ขยาย element ที่มี class `VList` หรือ virtual container
   - นับจำนวน DOM nodes ที่ render จริง
   - ✅ **Expected:** เห็น ~15-30 DOM nodes (ไม่ใช่ 10+ ทั้งหมด)

4. **Scroll Test:**
   - Scroll ขึ้น-ลงช้าๆ
   - สังเกต: scroll smooth ไหม, มี lag ไหม
   - ✅ **Expected:** Scroll ลื่น 60 FPS, ไม่กระตุก

5. **เพิ่ม 50 Mixed Messages:**
   - คลิกปุ่ม "Add 50 Mixed"
   - รอ messages โหลด (~1-2 วินาที)
   - Scroll ดูทั้งหมด
   - ✅ **Expected:** Performance ยังดีอยู่, ไม่ช้าลง

**ตรวจสอบ:**
- [ ] Mixed messages แสดงถูกต้องทุกประเภท
- [ ] DOM nodes ถูกจำกัดโดย virtualization (~30 nodes)
- [ ] Scroll performance ดี (60 FPS)
- [ ] ไม่มี DOM overlapping
- [ ] Image lazy loading ทำงาน (รูปโหลดเมื่อ scroll เข้ามา)

---

### Test Case 3: Stress Test (100+ ข้อความ)

**วัตถุประสงค์:** ทดสอบว่า Virtua รับมือกับ message จำนวนมากได้ดีไหม

**ขั้นตอน:**

1. **เริ่มต้นใหม่:**
   - คลิก "Clear All"

2. **Stress Test:**
   - คลิกปุ่ม "Stress Test (100 messages)" 🔴
   - รอ messages โหลด (~2-3 วินาที)
   - สังเกต: browser ค้าง/lag ไหม

3. **ตรวจสอบ Performance:**
   - เปิด Chrome DevTools → Performance tab
   - คลิก "Record" (วงกลมแดง)
   - Scroll ขึ้น-ลงในหน้าแชท ~10 วินาที
   - คลิก "Stop" recording
   - ดู FPS graph:
     - **Green (> 50 FPS):** ดีมาก ✅
     - **Yellow (30-50 FPS):** พอใช้ ⚠️
     - **Red (< 30 FPS):** มีปัญหา ❌

4. **ตรวจสอบ Memory:**
   - Chrome DevTools → Memory tab
   - Take Heap Snapshot
   - ดู memory usage
   - ✅ **Expected:** < 100 MB สำหรับ 100 messages

5. **Scroll to Top:**
   - Scroll ไปบนสุด (message แรก)
   - สังเกต: message โหลดครบไหม, มี blank space ไหม

6. **Scroll to Bottom:**
   - คลิก "Scroll to Bottom"
   - สังเกต: scroll ไปล่างสุดทันทีไหม
   - ✅ **Expected:** Jump ไปล่างได้ทันที, ไม่มี delay

**ตรวจสอบ:**
- [ ] 100 messages โหลดได้ภายใน 3 วินาที
- [ ] Scroll performance ยังดี (> 30 FPS)
- [ ] Memory usage สมเหตุสมผล (< 100 MB)
- [ ] ไม่มี blank spaces เมื่อ scroll เร็วๆ
- [ ] Images โหลดได้ทั้งหมดเมื่อ scroll ผ่าน

---

### Test Case 4: ทดสอบ DOM Overlapping (หัวใจหลัก)

**วัตถุประสงค์:** ตรวจสอบปัญหา DOM ทับกันที่คุณเคยเจอ

**ขั้นตอน:**

1. **เริ่มต้นใหม่:**
   - คลิก "Clear All"

2. **สร้าง Pattern ที่อาจทำให้เกิดปัญหา:**
   ```
   เพิ่มตามลำดับ:
   1. Add Image (รูป - dynamic height สูง)
   2. Add Text (ข้อความสั้น)
   3. Add Image (รูปอีกรูป)
   4. Add File (ไฟล์ - fixed height)
   5. Add Sticker (สติกเกอร์ - fixed height)
   6. Add Text (ข้อความยาว)
   7. Add Image (รูปอีกรูป)
   ```

3. **ตรวจสอบแต่ละ Message:**
   - Scroll ช้าๆ ดูทุก message
   - สังเกต:
     - รูปภาพแสดงครบไหม
     - Text ทับรูปไหม
     - File ทับ Sticker ไหม
     - Spacing ระหว่าง messages สม่ำเสมอไหม

4. **Scroll เร็วๆ:**
   - Scroll ขึ้น-ลงเร็วๆ หลายครั้ง
   - สังเกต: มี content jump ไหม (รูปโผล่มาทีหลัง)
   - ✅ **Expected:** ไม่มี jump, content stable

5. **ตรวจสอบด้วย DevTools:**
   - Right-click ที่ message → Inspect
   - ดู computed height ของแต่ละ message
   - ตรวจสอบว่า:
     - Text message: height ตามเนื้อหา (60-200px)
     - Image message: height ~180-250px
     - File message: height ~80-100px
     - Sticker message: height ~130-150px

6. **ทดสอบ Load More (ถ้าใช้ Backend Mode):**
   - Scroll ไปบนสุด
   - รอให้โหลด message เก่า
   - สังเกต: scroll position คงที่ไหม (ไม่กระโดด)
   - ✅ **Expected:** Scroll position ไม่เปลี่ยน, message เก่าปรากฏด้านบน

**เกณฑ์ผ่าน:**
- [ ] ไม่มี message ทับกันเลย (0 overlaps)
- [ ] Images โหลดครบและแสดงในตำแหน่งที่ถูกต้อง
- [ ] Spacing สม่ำเสมอ (~8-16px ระหว่าง messages)
- [ ] Scroll position stable (ไม่กระโดดเมื่อโหลด content ใหม่)
- [ ] ไม่มี blank spaces เมื่อ scroll

**เกณฑ์ไม่ผ่าน (มีปัญหา):**
- ❌ Message ทับกัน (เห็น 2 messages ซ้อนกัน)
- ❌ Images ปรากฏช้า ทำให้ message ข้างล่าง jump ขึ้น
- ❌ Spacing ไม่สม่ำเสมอ (บาง message ห่างมาก บาง message ชิดกัน)
- ❌ Scroll jump เมื่อรูปโหลด
- ❌ Blank spaces เมื่อ scroll เร็วๆ

---

### Test Case 5: ทดสอบกับข้อมูลจริง (Backend Mode)

**วัตถุประสงค์:** ทดสอบกับ conversation จริงที่มีข้อมูลใน database

**ขั้นตอน:**

1. **เข้าหน้าทดสอบด้วย Real Conversation:**
   ```
   http://localhost:5173/poc/chat-virtua-enhanced/69cd966b-c0f4-44bf-ae6f-f08eaf501e20
   ```
   (ใช้ conversationId จริงที่คุณมี)

2. **ตรวจสอบว่าอยู่ใน Backend Mode:**
   - ถ้าไม่ใช่ ให้คลิก "Use Backend"

3. **รอให้ Messages โหลด:**
   - สังเกต loading indicator
   - รอจนกว่า messages แสดงครบ

4. **ทดสอบ Scroll:**
   - Scroll ดูทุก message
   - ตรวจสอบ:
     - Text messages แสดงถูกต้อง
     - Images โหลดและแสดงผล
     - Files แสดง metadata
     - Stickers แสดงถูกต้อง

5. **ทดสอบ Load More:**
   - Scroll ไปบนสุด
   - รอให้ load more trigger (จะเห็น loading spinner)
   - สังเกต: message เก่าโหลดมาไหม
   - Scroll position คงที่ไหม

6. **ทดสอบ Real-time Updates (ถ้ามี WebSocket):**
   - ส่ง message ใหม่จากหน้าแชทหลัก
   - สังเกต: message ใหม่ปรากฏในหน้า POC ทันทีไหม
   - Auto-scroll to bottom ไหม

**ตรวจสอบ:**
- [ ] Messages จริงแสดงถูกต้องทุกประเภท
- [ ] Load more ทำงานและ preserve scroll position
- [ ] Real-time updates ทำงาน (ถ้ามี)
- [ ] Performance ดีกับข้อมูลจริง
- [ ] ไม่มี errors ใน console

---

## 🔍 จุดที่ต้องสังเกต (What to Watch For)

### 1. DOM Overlapping Indicators

**อาการที่บอกว่ามีปัญหา:**
- ✅ **ดี:** Messages เรียงกันเป็นระเบียบ, spacing สม่ำเสมอ
- ⚠️ **อาจมีปัญหา:** Images โหลดช้า ทำให้ content jump
- ❌ **มีปัญหา:** เห็น messages ทับกัน, text อยู่บนรูป, spacing ผิดปกติ

### 2. Performance Indicators

**FPS (Frames Per Second):**
- 60 FPS: สมบูรณ์แบบ ✅
- 30-60 FPS: ยอมรับได้ ⚠️
- < 30 FPS: มีปัญหา ❌

**Scroll Behavior:**
- Smooth scrolling: ดี ✅
- Occasional stutter: พอใช้ ⚠️
- Constant lag: แย่ ❌

### 3. Memory Usage

**ตาม Chrome Task Manager (Shift+Esc):**
- < 50 MB: ดีมาก ✅
- 50-100 MB: ดี ✅
- 100-200 MB: พอใช้ ⚠️
- > 200 MB: มีปัญหา ❌

---

## 🐛 ปัญหาที่อาจเจอและวิธีแก้ (Common Issues & Solutions)

### Issue 1: Images ไม่โหลด

**อาการ:**
- เห็นแต่ placeholder หรือ broken image icon

**สาเหตุ:**
- URL รูปภาพไม่ถูกต้อง (ใน Local Test Mode ใช้ picsum.photos)
- CORS issues

**วิธีแก้:**
- ตรวจสอบ console errors
- ลองใช้ Backend Mode แทน
- ตรวจสอบ network tab ว่า images load หรือไม่

### Issue 2: Scroll Jump เมื่อรูปโหลด

**อาการ:**
- Scroll position กระโดดเมื่อรูปภาพโหลดเสร็จ

**สาเหตุ:**
- Image ไม่ได้กำหนด aspect ratio หรือ minHeight
- Virtua ไม่ได้คำนวณความสูงล่วงหน้า

**วิธีแก้:**
- ตรวจสอบว่า ImageMessage component มี `minHeight` และ `aspectRatio`
- ใช้ skeleton placeholder ขณะโหลด

### Issue 3: Messages ทับกัน

**อาการ:**
- เห็น 2 messages อยู่ในพื้นที่เดียวกัน

**สาเหตุ:**
- Wrapper components มากเกินไป
- CSS conflicts
- Virtua `shift` prop ไม่ทำงาน

**วิธีแก้:**
- ตรวจสอบ DOM structure ด้วย DevTools
- ตรวจสอบ CSS ว่ามี `position: absolute` ที่ไม่ควรมี
- ตรวจสอบว่า `shift={isPrependRef.current}` ทำงานถูกต้อง

### Issue 4: Performance ช้า

**อาการ:**
- Scroll lag, FPS ต่ำ

**สาเหตุ:**
- Re-render บ่อยเกินไป
- Components ไม่ได้ memoize
- DOM nodes มากเกินไป

**วิธีแก้:**
- ใช้ React DevTools Profiler ตรวจสอบ re-renders
- เพิ่ม `React.memo` ให้ message components
- ตรวจสอบว่า virtualization ทำงานจริง (DOM nodes ควร ~30 เท่านั้น)

---

## 📊 เกณฑ์ความสำเร็จ (Success Criteria)

### ผ่านการทดสอบถ้า:

- ✅ **ไม่มี DOM overlapping** ใน Test Case 4
- ✅ **Scroll performance > 30 FPS** ใน Test Case 3
- ✅ **Memory < 100 MB** สำหรับ 100 messages
- ✅ **Scroll to bottom/top ทันที** ไม่มี delay
- ✅ **Load more preserve scroll position** ใน Backend Mode
- ✅ **Images โหลดและแสดงถูกต้อง** ทุกครั้ง
- ✅ **Mixed messages แสดงถูกต้อง** ไม่มีปัญหา spacing

### ไม่ผ่านการทดสอบถ้า:

- ❌ พบ DOM overlapping > 0 ครั้ง
- ❌ FPS < 30 ใน Test Case 3
- ❌ Memory > 200 MB
- ❌ Scroll jump มากกว่า 1 ครั้งต่อ 10 tests
- ❌ Images ไม่โหลดหรือแสดงผิดตำแหน่ง
- ❌ Console มี errors/warnings

---

## 📸 วิธีบันทึกผลการทดสอบ (How to Document Results)

### 1. Screenshot ของปัญหา

ถ้าเจอปัญหา:
1. กด `PrtScn` หรือ `F12` → Screenshot
2. วงกลมส่วนที่มีปัญหา
3. บันทึกพร้อมกับ:
   - Test case ที่เจอ
   - Steps to reproduce
   - Expected vs. Actual

### 2. บันทึก Performance Metrics

สร้างตารางผลการทดสอบ:

```markdown
| Test Case | Messages Count | FPS | Memory (MB) | Issues Found | Pass/Fail |
|-----------|---------------|-----|-------------|--------------|-----------|
| TC1       | 4             | 60  | 25          | None         | ✅ Pass   |
| TC2       | 10            | 60  | 35          | None         | ✅ Pass   |
| TC2       | 50            | 55  | 60          | None         | ✅ Pass   |
| TC3       | 100           | 45  | 85          | None         | ✅ Pass   |
| TC4       | 7             | 60  | 30          | 1 overlap    | ❌ Fail   |
```

### 3. Video Recording (ถ้าเจอปัญหาซับซ้อน)

ใช้ OBS Studio หรือ Windows Game Bar (Win+G) record หน้าจอขณะทดสอบ

---

## 🎓 สรุป (Summary)

หน้าทดสอบนี้ช่วยให้คุณ:
1. **ทดสอบ Virtua** กับ message หลายประเภท
2. **ตรวจจับปัญหา DOM overlapping** ก่อนนำไปใช้จริง
3. **วัด Performance** และ Memory usage
4. **เปรียบเทียบ** กับ implementation อื่นๆ (POC อื่น)

หลังจากทดสอบเสร็จ:
- ✅ ถ้าผ่าน → ใช้ Virtua ได้เลยใน production
- ⚠️ ถ้าพบปัญหาเล็กน้อย → แก้ไขและทดสอบใหม่
- ❌ ถ้าไม่ผ่าน → พิจารณา refactor หรือใช้ library อื่น

---

**Happy Testing! 🚀**

หากพบปัญหาหรือมีคำถาม สามารถเปิด issue ใน GitHub หรือแจ้งทีมพัฒนาได้เลยครับ
